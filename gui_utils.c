#include "gui_utils.h"

GtkWidget* get_widget_by_name(GtkWidget* parent, const char* name) {
    printf("Not implemented, using hardcoded solution.\n");
    exit(0);
}

GtkWidget* widget_get_nth_child(GtkWidget* parent, unsigned int n) {
    // if (GTK_IS_BOX())
    // TODO: use combiation of parent.first_child() and first_child.next_sibling()
    printf("Not implemented, using hardcoded solution.\n");
    exit(0);
}
